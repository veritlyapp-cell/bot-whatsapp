{
    "functions": {
        "source": "functions",
        "runtime": "nodejs20",
        "predeploy": [
            "npm --prefix \"$RESOURCE_DIR\" run build"
        ]
    },
    "firestore": {
        "rules": "lia-frontend/firestore.rules",
        "indexes": "lia-frontend/firestore.indexes.json"
    }
}